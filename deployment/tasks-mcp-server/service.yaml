apiVersion: v1
kind: Service
metadata:
  name: tasks-mcp-server
  namespace: default
spec:
  selector:
    app: tasks-mcp-server
  ports:
    - name: mcp
      port: 8000
      targetPort: 8000
  type: ClusterIP
